<li> {{#link-to 'question' question.id}} <span class="boldText"> {{question.text}} </span> {{/link-to}}
  <br>
  {{#if isAuthorShowing}}
  <br>
    <p> {{question.author}} </p>
  {{else}}
    <button class="btn btn-warning" {{action 'authorShow'}}> Show Author </button>
  {{/if}}
  <button class="btn btn-success" {{action 'addToFavorites' question}}> Add to favorites </button>
</li>
  <hr class="short">
