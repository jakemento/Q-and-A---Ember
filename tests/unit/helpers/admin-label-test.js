import { adminLabel } from 'q-and-a/helpers/admin-label';
import { module, test } from 'qunit';

module('Unit | Helper | admin label');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = adminLabel([42]);
  assert.ok(result);
});
